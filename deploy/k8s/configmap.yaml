apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: app-platform
data:
  # 应用配置
  APP_ENV: "production"
  APP_PORT: "8080"
  
  # 日志配置
  LOG_LEVEL: "info"
  LOG_FORMAT: "json"
  
  # 跨域配置
  CORS_ORIGINS: "*"
  
  # 其他配置
  TZ: "Asia/Shanghai"
