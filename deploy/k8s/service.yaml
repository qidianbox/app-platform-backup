apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: app-platform
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: backend
